# 复制本文件为 .env 后修改（Vite 不会读取 .env.example）
# cp .env.example .env
#
# 生产部署：复制为 .env.production 或在 frontend/.env.production 中配置后执行 npm run build
# cp .env.example .env.production

VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
